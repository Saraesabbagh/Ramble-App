import "./Page.css"
import { Navbar } from "../atomic-components/Navbar";
import React from "react";

export const Page = (props) => {
    return (

    <Navbar />
    // {props.children}
    // <Footer />
    )
}